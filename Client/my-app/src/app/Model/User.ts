export class User { //export default class User {
    id: number
    userName: string
    userProfile: string
    properties: string
    creator: string
    creation_date: Date
    modifier: string
	modifiedDate: Date
    suspended: boolean

    //user_level: number
}